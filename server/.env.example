# PostgreSQL connection used by the API and Drizzle Kit.
DATABASE_URL=postgresql://<username>:<password>@<host>:5432/<database>?sslmode=require

# Exact frontend origin. Use the stable HTTPS Vercel origin in production.
CLIENT_ORIGIN=http://localhost:5173

NODE_ENV=development
PORT=3000
